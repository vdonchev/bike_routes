{% extends '_base.html.twig' %}

{% block body %}
    {% include '_nav.html.twig' with {'user': user} %}

    <div class="container mt-4">
        <div class="row">
            <div class="col-md-6 offset-md-3 col-lg-4 offset-lg-4">

                <h1 class="h4">Хей, {{ user.name }}!</h1>
                <hr>
                <p>Твоето потребителско име е: <b>{{ user.username }}</b></p>
                <p>
                    <a href="/password" class="btn btn-primary">Смени паролата</a>
                    <a href="/logout" class="btn btn-primary">Излез</a>
                </p>
            </div>
        </div>
    </div>
{% endblock %}